using System.Linq;

void Main()
{
	var prefix = "-";
	var array = new string[] { "alpha", "beta", "gamma" };

	var result = array.Select(new LambdaScope0() { prefix = prefix }.SkipWhile1);
}

public struct LambdaScope0
{
	public string prefix;
	public string SkipWhile1(string s) { return prefix + s.SkipWhile(Lambda_func0); }
}

static bool Lambda_func0(char c) { return c == 'a'; }
